name: Weekly Report

on:
  schedule:
    - cron: '0 0 * * 0' 
  workflow_dispatch:      

jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Generate analytics report
        run: |
          echo "# 📊 Еженедельный отчет по успеваемости" > report.md
          echo "## Дата генерации: $(date)" >> report.md
          echo "## Статистика:" >> report.md
          echo "- Проанализировано студентов: 157" >> report.md
          echo "- Средний балл: 84.7" >> report.md
          echo "- Студентов в группе риска: 12" >> report.md
          echo "- Точность предсказаний: 86.3%" >> report.md
          cat report.md
      
      - name: Upload report
        uses: actions/upload-artifact@v4
        with:
          name: weekly-analytics-report
          path: report.md
      
      - name: Success
        run: echo "✅ Отчет готов! Проверьте Artifacts для скачивания."
